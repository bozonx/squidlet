name: DigitalInput.driver
main: DigitalInput.driver.ts
type: system

# TODO: remove
drivers:
  - Digital_local.driver

props:
  pullup:
    type: boolean
    default: false
  pulldown:
    type: boolean
    default: false

  ### Digital base props
  pin:
  gpio:
