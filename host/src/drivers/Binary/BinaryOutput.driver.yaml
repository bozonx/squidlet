name: BinaryOutput.driver
main: BinaryOutput.driver.ts
type: system
drivers:
  - DigitalOutput.driver

props:
  blockTime:
    type: number
    default: 0
  blockMode:
    type: refuse | defer
    default: refuse

  ### DigitalOutput driver props
  initial:

  ### Digital base props
  pin:
  invert:
  gpio:
