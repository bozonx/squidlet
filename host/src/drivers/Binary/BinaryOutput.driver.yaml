name: BinaryOutput.driver
main: BinaryOutput.driver.ts
type: system
drivers:
  - DigitalOutput.driver

props:
  blockTime:
    type: number
    default: 0
  blockMode:
    type: refuse | defer
    default: refuse
  initial:
    type: 1 | 0 | low | high
    default: 0
  invert:
    type: boolean
    default: false

  # DigitalOutputDriverProps
  #initial:

  ### Digital base props
  pin:
  gpio:
