#$extends: gpioBaseProps

# debounce time in ms
debounce:
  type: number
  default: 0
edge:
  type: rising | falling | both
  default: rising
# timeout after received signal. At this time it doesn't receive any signals.
blockTime:
  type: number
  default: 300
