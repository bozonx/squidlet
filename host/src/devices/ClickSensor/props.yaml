# debounce time in ms
debounce:
  type: number
  default: 0
edge:
  type: rising | falling | both
  default: rising
# timeout after received signal. At this time it doesn't receive any signals.
blockTime:
  type: number
  default: 300

### other BinaryInput driver props
invertOnPullup:

### DigitalInput driver props
pullup:
pulldown:

### Digital base props
pin:
invert:
gpio:

### Device base props
statusRepublishInterval:
configRepublishInterval:
