# Publish logical 1 on down or up events. Or publish 1 and 0 which are represent the state of button
publish:
  type: down | up | state
  default: state

# BinaryInput
blockTime:
invertOnPullup:
invert:

### DigitalPinInput driver props
pullup:
pulldown:

### DigitalPin base props
pin:
source:


### Device base props
statusRepublishInterval:
configRepublishInterval:
