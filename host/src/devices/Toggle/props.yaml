# debounce which is used on turn or toggle actions
actionDebounce:
  type: number
  default: 20
# timeout after received signal. At this time it doesn't receive any signals.
blockTime:
  default: 50
invert:

### BinaryInput driver props
# debounce time in ms
debounce:
  default: 25
edge:
invertOnPullup:

### DigitalInput driver props
pullup:
pulldown:

### Digital base props
pin:
gpio:
#invert

### Device base props
statusRepublishInterval:
configRepublishInterval:
