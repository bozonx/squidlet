$extends: gpioBaseProps

# debounce time in ms
debounce:
  type: number
  default: 20
# timeout after received signal. At this time it doesn't receive any signals.
blockTime:
  type: number
  default: 50
