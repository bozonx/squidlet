export function eachFileRecursivelly(rootDir: string, cb: (pathToFile: string) => void) {
  // TODO: add
  // TODO: проверить что папка существует и есть файлы
}

export function makeModuleName(pathToFile: string, removeRoot: string, newRoot: string): string {
  // TODO: add
}
