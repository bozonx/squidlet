name: WsClient
main: WsClient.ts
system: true

ios:
  - WebSocketClient

props:
#  host:
#    type: string
#    default: localhost
#  port:
#    type: number
#    default: 8080
  url:
    type: string
    #required: true
    default: "ws://localhost:8080"
  autoReconnect:
    type: boolean
    default: false
  reconnectTimeoutMs:
    type: number
    # in ms
    default: 1000
  maxTries:
    type: number
    default: -1
