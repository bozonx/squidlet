name: ImpulseInput.driver
main: ImpulseInput.driver.ts
type: system

drivers:
  - DigitalPinInput

props:
  impulseLength:
    type: number
    default: 100
  blockTime:
    type: number
    default: 0
#  throttle:
#    type: number?
  invertOnPullup:
    type: boolean
    default: true
  invert:
    type: boolean
    default: false

  ### DigitalPinInput driver
  debounce:
    default: 0
  doubleCheck:
  edge:
  pullup:
  pulldown:

  ### DigitalPin base
  pin:
  source:
