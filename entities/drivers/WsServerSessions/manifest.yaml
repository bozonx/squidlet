name: WsServerSessions
main: WsServerSessions.ts

ios:
  - WebSocketServer

props:
  host:
    type: string
    default: localhost
  port:
    type: number
    default: 8080
  # session expiration time in seconds
  expiredSec:
    type: number
    default: 30
