name: WebSocketServerRouter
main: WebSocketServerRouter.ts
system: true

drivers:
  - WebSocketServer

props:
  host:
    type: string
    default: localhost
  port:
    type: number
    required: true
