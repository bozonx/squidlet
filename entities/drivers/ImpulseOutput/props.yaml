impulseLength:
  type: number
  default: 20
blockTime:
  type: number
  default: 0
blockMode:
  type: refuse | defer
  default: refuse
invert:
  type: boolean
  default: false

### DigitalPin base props
pin:
source:
