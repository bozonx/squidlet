$base: 'drivers.DigitalPinOutput'

impulseLength:
  type: number
  default: 20
blockTime:
  type: number
  default: 0
blockMode:
  type: '"refuse" | "defer"'
  default: refuse
invert:
  type: boolean
  default: false

### DigitalPinOutput driver props
# initialLevel - don't use it, it will be calculated and reassigned
#
#### DigitalPin base props
#pin: {}
#source: {}
