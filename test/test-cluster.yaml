plugins: []

hosts:
  master:
    platform: lowjs
    machine: esp32wrover
    config:
      #storageDir: './masterStorage'

    logger:

    devices:
      bedroom:
        people-count-sensor:
          device: FakePeopleCount
hostDefaults:
