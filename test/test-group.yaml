plugins: []

hostDefaults: {}

hosts:
  - id: testHost
    platform: nodejs
    machine: x86
    config:
      someParam: 1
      #storageDir: './masterStorage'

    #logger: {}

    devices:
      bedroom:
        people-count-sensor:
          device: FakePeopleCount
